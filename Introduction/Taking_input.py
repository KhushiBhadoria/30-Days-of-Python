'''input() - Python provides a built-in function called input which takes input from the user. It takes input from the 
user and convert it into string. The type of the returned object will always be string <type 'str>.
'''
name=input('What is your name?\n') # \n -> newline -> It causes a line break 
print(name) #Khushi
num=input('Enter number:')

# Printing type of input value
# type() -> It returns the data type of the object
print(type(name)) # type->str
print(type(name)) # type->str

